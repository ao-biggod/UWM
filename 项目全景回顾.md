# UWM PushT 项目全景回顾

> 从 DP 基线复现到定位 AdaLN 架构瓶颈的完整研究历程
> 时间跨度：2026-05-28 至 2026-06-10

---

## 第一章：背景——两个项目的来龙去脉

### 1.1 Diffusion Policy（扩散策略）—— 数学原理

Diffusion Policy（简称 DP）是由 Columbia、TRI（Toyota Research Institute）、MIT、Stanford 等机构研究者于 2023 年合作提出的一种机器人模仿学习算法。它的核心思想是将**扩散模型**（Diffusion Model）用于机器人动作生成。

传统的模仿学习通常直接预测动作（给定观测，输出动作），而 Diffusion Policy 的做法是：
1. 从纯高斯噪声开始
2. 以当前观测（相机图像+机器人状态）为条件
3. 逐步去噪，经过多轮迭代精炼，生成一段连续的动作序列
4. 只执行前几步，然后重新观测、重新规划（滚动时域控制）

DP 的 **CNN 版本**使用 **1D 时序卷积/UNet + FiLM 条件化**。UNet 是一个多尺度的一维卷积网络，FiLM 是一种简单的条件调制方式（对特征进行缩放和平移）。DP 论文同时提出了 **Transformer 版本**（`TransformerForDiffusion`），通过 cross-attention 注入观测特征，尤其适用于高频动作变化或复杂状态空间的任务。本文后续的外部正对照实验 E3/E4 即使用此 Transformer 版本。

在理解 Diffusion Policy 之前，必须先理解扩散模型本身的数学原理。

---

#### 1.1.1 扩散模型的核心思想

扩散模型是一类生成模型，它受到非平衡热力学的启发。整个框架包含两个过程：

- **前向过程（Forward Process / Diffusion Process）**：对原始数据逐步添加高斯噪声，直到数据完全变为纯噪声。这个过程是固定的、不可学习的。
- **逆向过程（Reverse Process / Denoising Process）**：从纯噪声出发，逐步去除噪声，恢复出有意义的数据。这个过程需要通过学习来近似。

直觉理解：前向过程就像把一滴墨水滴入一杯清水中，墨水逐渐扩散直至均匀分布；逆向过程就像让这杯均匀的墨水"逆流回去"重新聚成一滴——这需要一个学习到的模型来驱动。

---

#### 1.1.2 前向扩散过程：逐步加噪

设原始数据为 $\mathbf{x}_0$（在 DP 中，这是一段动作序列，例如 16 步 × 2 维 = 32 维的向量）。

定义一个方差调度 $\{\beta_t \in (0,1)\}_{t=1}^{T}$，其中 T 是总扩散步数（DP 中通常为 100）。$\beta_t$ 通常从小到大排列（例如从 $10^{-4}$ 线性增长到 0.02）。

**前向过程的单步转移**：

$$
q(\mathbf{x}_t \mid \mathbf{x}_{t-1}) = \mathcal{N}(\mathbf{x}_t; \sqrt{1 - \beta_t}\,\mathbf{x}_{t-1}, \beta_t \mathbf{I})
$$

这个等式的含义是：第 t 步的噪声数据 $\mathbf{x}_t$ 服从一个高斯分布，其均值为 $\sqrt{1-\beta_t}\,\mathbf{x}_{t-1}$（前一步的数据被略微缩小），方差为 $\beta_t$（新增的噪声强度）。

- 当 $\beta_t$ 很小时，$\sqrt{1-\beta_t} \approx 1$，数据变化不大，噪声添加很少
- 当 $\beta_t$ 较大时，数据被更多缩小，噪声成分增加
- 随着 t 从 1 到 T，噪声占比越来越大，原始信号越来越弱

**从原始数据直接跳到任意时间步**：由于高斯分布的可加性，前向过程有一个极好的性质——可以用一个闭式解直接从 $\mathbf{x}_0$ 计算任意 t 时刻的 $\mathbf{x}_t$，无需逐步迭代。

定义 $\alpha_t = 1 - \beta_t$，以及 $\bar{\alpha}_t = \prod_{s=1}^{t} \alpha_s$（累积信号保留率）。则有：

$$
q(\mathbf{x}_t \mid \mathbf{x}_0) = \mathcal{N}(\mathbf{x}_t; \sqrt{\bar{\alpha}_t}\,\mathbf{x}_0, (1 - \bar{\alpha}_t)\mathbf{I})
$$

**推导过程**（重参数化技巧）：

重参数化技巧允许我们将采样操作写成确定性变换加上随机噪声：

$$
\mathbf{x}_t = \sqrt{\alpha_t}\,\mathbf{x}_{t-1} + \sqrt{1-\alpha_t}\,\boldsymbol{\epsilon}_{t-1}, \quad \boldsymbol{\epsilon}_{t-1} \sim \mathcal{N}(0, \mathbf{I})
$$

不推导全式，核心在于：递推代入可知：

$$
\begin{aligned}
\mathbf{x}_t &= \sqrt{\alpha_t}\,\mathbf{x}_{t-1} + \sqrt{1-\alpha_t}\,\boldsymbol{\epsilon}_{t-1} \\
&= \sqrt{\alpha_t \alpha_{t-1}}\,\mathbf{x}_{t-2} + \sqrt{\alpha_t(1-\alpha_{t-1})}\,\boldsymbol{\epsilon}_{t-2} + \sqrt{1-\alpha_t}\,\boldsymbol{\epsilon}_{t-1}
\end{aligned}
$$

由于 $\boldsymbol{\epsilon}_{t-2}$ 和 $\boldsymbol{\epsilon}_{t-1}$ 是独立高斯变量，两个独立高斯噪声之和仍为高斯噪声，其方差相加。最终收敛到：

$$
\mathbf{x}_t = \sqrt{\bar{\alpha}_t}\,\mathbf{x}_0 + \sqrt{1-\bar{\alpha}_t}\,\boldsymbol{\epsilon}, \quad \boldsymbol{\epsilon} \sim \mathcal{N}(0, \mathbf{I})
$$

**关键洞察**：给定原始数据 $\mathbf{x}_0$，任意时间步的加噪数据 $\mathbf{x}_t$ 可以直接通过对 $\mathbf{x}_0$ 的缩放加上合适强度的高斯噪声获得。这是扩散模型训练效率的关键——不需要逐步模拟前向过程。

当 t 趋近于 T 时，$\bar{\alpha}_T \to 0$，则 $\mathbf{x}_T \approx \mathcal{N}(0, \mathbf{I})$，几乎完全是纯噪声。

---

#### 1.1.3 逆向去噪过程：逐步去噪

逆向过程是从噪声恢复到数据的过程。真实的后验分布 $q(\mathbf{x}_{t-1} \mid \mathbf{x}_t)$ 是未知的（它需要知道整个数据分布），但我们可以用一个神经网络 $\boldsymbol{\epsilon}_\theta$ 来近似它。

在给定原始数据 $\mathbf{x}_0$ 的条件下，后验分布有闭式解：

$$
q(\mathbf{x}_{t-1} \mid \mathbf{x}_t, \mathbf{x}_0) = \mathcal{N}(\mathbf{x}_{t-1}; \tilde{\boldsymbol{\mu}}_t(\mathbf{x}_t, \mathbf{x}_0), \tilde{\beta}_t \mathbf{I})
$$

其中：

$$
\tilde{\boldsymbol{\mu}}_t(\mathbf{x}_t, \mathbf{x}_0) = \frac{\sqrt{\bar{\alpha}_{t-1}}\beta_t}{1-\bar{\alpha}_t}\mathbf{x}_0 + \frac{\sqrt{\alpha_t}(1-\bar{\alpha}_{t-1})}{1-\bar{\alpha}_t}\mathbf{x}_t
$$

$$
\tilde{\beta}_t = \frac{1-\bar{\alpha}_{t-1}}{1-\bar{\alpha}_t}\beta_t
$$

但我们没有 $\mathbf{x}_0$（那是我们要生成的！），所以用神经网络来预测。**关键思路**：并非直接预测 $\mathbf{x}_0$，而是预测添加到 $\mathbf{x}_0$ 上的噪声 $\boldsymbol{\epsilon}$。

利用 $\mathbf{x}_t = \sqrt{\bar{\alpha}_t}\,\mathbf{x}_0 + \sqrt{1-\bar{\alpha}_t}\,\boldsymbol{\epsilon}$，可以解出：

$$
\mathbf{x}_0 = \frac{1}{\sqrt{\bar{\alpha}_t}}(\mathbf{x}_t - \sqrt{1-\bar{\alpha}_t}\,\boldsymbol{\epsilon})
$$

将上式代入 $\tilde{\boldsymbol{\mu}}_t$ 的公式（代入后化简），得到用预测噪声表达的均值：

$$
\tilde{\boldsymbol{\mu}}_t(\mathbf{x}_t, \mathbf{x}_0) = \frac{1}{\sqrt{\alpha_t}}\left(\mathbf{x}_t - \frac{1-\alpha_t}{\sqrt{1-\bar{\alpha}_t}}\,\boldsymbol{\epsilon}\right)
$$

因此，神经网络只需要学习预测噪声 $\boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t)$，即给定加噪数据 $\mathbf{x}_t$ 和时间步 t，预测被添加的噪声 $\boldsymbol{\epsilon}$。

**逆向采样步骤**（DDPM 采样算法）：

单步去噪的采样公式（代入均值并加入方差项）：

$$
\mathbf{x}_{t-1} = \frac{1}{\sqrt{\alpha_t}}\left(\mathbf{x}_t - \frac{1-\alpha_t}{\sqrt{1-\bar{\alpha}_t}}\,\boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t)\right) + \sigma_t \mathbf{z}, \quad \mathbf{z} \sim \mathcal{N}(0, \mathbf{I})
$$

其中 $\sigma_t^2 = \tilde{\beta}_t$。当 t > 1 时添加随机噪声 $\mathbf{z}$ 以维持随机性；当 t = 1 时不加噪声直接输出 $\mathbf{x}_0$。

从纯噪声 $\mathbf{x}_T \sim \mathcal{N}(0, \mathbf{I})$ 开始，重复上述采样步骤 T 次（t 从 T 递减到 1），最终得到生成的样本 $\mathbf{x}_0$。

---

#### 1.1.4 训练目标：噪声预测损失

训练目标是让预测噪声尽可能逼近真实噪声。最简单的形式是均方误差（MSE）：

$$
\mathcal{L}_{\text{simple}} = \mathbb{E}_{t, \mathbf{x}_0, \boldsymbol{\epsilon}}\left[\|\boldsymbol{\epsilon} - \boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t)\|_2^2\right]
$$

完整推导（从变分下界/ELBO 出发）：

扩散模型可以看作一个隐变量模型 $p_\theta(\mathbf{x}_0) = \int p_\theta(\mathbf{x}_{0:T})\,d\mathbf{x}_{1:T}$，训练目标是最大化数据对数似然的变分下界（ELBO）：

$$
\mathbb{E}_{q(\mathbf{x}_0)}[\log p_\theta(\mathbf{x}_0)] \geq \mathbb{E}_{q(\mathbf{x}_{0:T})}\left[\log\frac{p_\theta(\mathbf{x}_{0:T})}{q(\mathbf{x}_{1:T}\mid\mathbf{x}_0)}\right]
$$

将联合分布展开：

$$
p_\theta(\mathbf{x}_{0:T}) = p(\mathbf{x}_T)\prod_{t=1}^{T}p_\theta(\mathbf{x}_{t-1}\mid\mathbf{x}_t)
$$

$$
q(\mathbf{x}_{1:T}\mid\mathbf{x}_0) = \prod_{t=1}^{T}q(\mathbf{x}_t\mid\mathbf{x}_{t-1})
$$

经过 KL 散度的分解（此处省略冗长的代数推导），ELBO 可重写为：

$$
\begin{aligned}
L &= \mathbb{E}_q\left[\underbrace{D_{\text{KL}}(q(\mathbf{x}_T\mid\mathbf{x}_0)\,\|\,p(\mathbf{x}_T))}_{L_T} \right. \\
&\quad + \sum_{t=2}^{T}\underbrace{D_{\text{KL}}(q(\mathbf{x}_{t-1}\mid\mathbf{x}_t,\mathbf{x}_0)\,\|\,p_\theta(\mathbf{x}_{t-1}\mid\mathbf{x}_t))}_{L_{t-1}} \\
&\quad \left. - \underbrace{\log p_\theta(\mathbf{x}_0\mid\mathbf{x}_1)}_{L_0}\right]
\end{aligned}
$$

关键项 $L_{t-1}$ 是两个高斯分布之间的 KL 散度——因为 $q(\mathbf{x}_{t-1}\mid\mathbf{x}_t,\mathbf{x}_0)$ 和 $p_\theta(\mathbf{x}_{t-1}\mid\mathbf{x}_t)$ 都是高斯分布（我们设定 $p_\theta$ 的方差为 $\sigma_t^2\mathbf{I}$ 或将方差也参数化）。两个高斯分布的 KL 散度有闭式解，化简后正比于均值之差的平方。

将均值的表达式代入并化简（引入重参数化），最终所有 t-1 项合并为：

$$
L_{t-1} = \mathbb{E}_{\mathbf{x}_0,\boldsymbol{\epsilon}}\left[\frac{\beta_t^2}{2\sigma_t^2\alpha_t(1-\bar{\alpha}_t)}\|\boldsymbol{\epsilon} - \boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t)\|_2^2\right] + C
$$

其中 C 是与 $\theta$ 无关的常数。

**Ho et al. (2020)** 发现，丢弃前面的权重系数，直接使用简化的无加权 MSE 版本反而效果更好：

$$
\mathcal{L}_{\text{simple}}(\theta) = \mathbb{E}_{t\sim[1,T], \mathbf{x}_0\sim q(\mathbf{x}_0), \boldsymbol{\epsilon}\sim\mathcal{N}(0,\mathbf{I})}\left[\|\boldsymbol{\epsilon} - \boldsymbol{\epsilon}_\theta(\sqrt{\bar{\alpha}_t}\mathbf{x}_0 + \sqrt{1-\bar{\alpha}_t}\boldsymbol{\epsilon}, t)\|_2^2\right]
$$

**训练算法的每步流程**：
1. 从数据集中采样一个干净样本 $\mathbf{x}_0$（一段专家动作序列）
2. 随机采样一个扩散时间步 $t \sim \text{Uniform}(1, T)$
3. 采样高斯噪声 $\boldsymbol{\epsilon} \sim \mathcal{N}(0, \mathbf{I})$
4. 计算加噪数据 $\mathbf{x}_t = \sqrt{\bar{\alpha}_t}\mathbf{x}_0 + \sqrt{1-\bar{\alpha}_t}\boldsymbol{\epsilon}$
5. 用神经网络预测噪声 $\hat{\boldsymbol{\epsilon}} = \boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t)$
6. 计算损失 $\|\boldsymbol{\epsilon} - \hat{\boldsymbol{\epsilon}}\|_2^2$ 并反向传播
7. 更新网络参数

这个训练方式的设计优势在于：**不需要对抗训练，不需要手工设计的复杂损失函数**——MSE 回归加上随机时间步采样即可。对无条件 DDPM 图像生成而言，训练甚至不需要额外人工标签（每张图本身就是目标）；但在 Diffusion Policy 中，训练样本仍然是专家示教中的观测-动作配对 $(\mathbf{O}_t, \mathbf{A}_t)$，即 DDPM 框架本身不要求标签，但模仿学习任务天然依赖 demonstration 数据。

---

#### 1.1.5 方差调度（Noise Schedule）的含义

方差调度 $\{\beta_t\}_{t=1}^{T}$ 决定了前向过程中的噪声注入速度，直接影响信号保留率 $\bar{\alpha}_t$ 的衰减曲线。

- **线性调度（Linear Schedule）**：$\beta_t$ 从 $\beta_1$ 线性增长到 $\beta_T$，如 $\beta_t = 10^{-4} + (0.02 - 10^{-4})\frac{t-1}{T-1}$

- **余弦调度（Cosine Schedule）**：$\bar{\alpha}_t$ 按余弦函数衰减：$\bar{\alpha}_t = \frac{f(t)}{f(0)}$，其中 $f(t) = \cos\left(\frac{t/T + s}{1+s}\cdot\frac{\pi}{2}\right)^2$。余弦调度在中间阶段保留更多信号，在两端更快衰减，通常效果更优。

不同调度的直观差异：线性调度在早期噪声添加很快（信号迅速衰减），而余弦调度在中期信号衰减更均匀。对于机器人动作这种需要精确空间信息的数据，余弦调度通常更合适。

---

#### 1.1.6 DDIM：确定性加速采样

DDPM 的采样需要 T 步（通常 100 步），推理速度慢。Song et al. (2021) 提出了 **DDIM**（去噪扩散隐式模型），允许在远少于 T 的步数内完成采样。

**DDIM 的核心公式**：

从 $\mathbf{x}_t$ 到 $\mathbf{x}_{t-1}$ 的更新（使用预测的 $\mathbf{x}_0$）：

$$
\mathbf{x}_0^{\text{pred}} = \frac{\mathbf{x}_t - \sqrt{1-\bar{\alpha}_t}\,\boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t)}{\sqrt{\bar{\alpha}_t}}
$$

$$
\mathbf{x}_{t-1} = \sqrt{\bar{\alpha}_{t-1}}\,\mathbf{x}_0^{\text{pred}} + \sqrt{1 - \bar{\alpha}_{t-1} - \sigma_t^2}\,\boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t) + \sigma_t\mathbf{z}
$$

当 $\sigma_t = 0$ 时，采样过程变为**完全确定性**——给定同样的 $\mathbf{x}_T$，每次生成的 $\mathbf{x}_0$ 完全相同。这是 DDIM 与 DDPM 的本质区别：DDPM 是随机过程，DDIM（σ=0）是确定性映射。

在实际使用中，我们不再需要走完所有 T 步，而是从训练步序列中选取一个子序列 $\{\tau_0, \tau_1, \ldots, \tau_{S-1}\}$（如从 100 个训练步中均匀间隔取 10 个时间步），从 $\tau_0 \approx T$ 到 $\tau_{S-1} = 0$ 逐步去噪，大幅加速推理。

> **关于推理步数**：DP 论文中不同任务使用不同的推理步数配置——真实机器人任务常用 10-16 步 DDIM 以满足实时性（~0.1s 延迟），模拟任务有时用完整的 100 步 DDPM。本项目 UWM 使用 10 步 DDIM，DP 外控 E4 使用 100 步 DDPM。推理步数差异是分数差距的**潜在因素之一**，但不能简单归因于"DP 一律用 100 步"——具体取决于任务配置。

---

#### 1.1.7 条件生成：以观测为条件的扩散

以上描述的都是无条件生成。Diffusion Policy 需要的是**条件生成**：以当前观测（图像+机器人状态）为条件，生成对应的动作序列。

设观测为 $\mathbf{O}_t$（第 t 个时间窗的观测，包含 2 帧图像和 2 帧 agent 位置），要生成的动作序列为 $\mathbf{A}_t$（未来 16 步的动作）。

**条件扩散的目标**：学习 $p(\mathbf{A}_t^0 \mid \mathbf{O}_t)$，即给定观测的条件下，动作序列的分布。

DP 采用**直接条件噪声预测**，无需 classifier-free guidance：

$$
\boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t, \mathbf{O}_t)
$$

即神经网络的输入除了加噪动作 $\mathbf{x}_t$ 和时间步 t，**还拼接或调制了观测特征**（在 CNN/UNet 版本中通过 FiLM 层注入，在 Transformer 版本中通过 cross-attention 注入）。注意：这是 DP 的标准条件化方式，不使用 classifier-free guidance（CFG 是其他条件扩散模型中常用的增强技巧，但并非 DP 论文的默认机制）。

**条件注入在 UNet 中的具体实现（FiLM）**：

$$
\text{FiLM}(x \mid \gamma, \beta) = \gamma \odot x + \beta
$$

观测通过视觉编码器（ResNet18）编码为特征向量，然后通过线性层预测出每个 UNet 层的 $\gamma$（缩放因子）和 $\beta$（平移因子），对特征进行逐通道调制。这就是**条件化**——模型根据观测来决定如何调整特征表达以正确去噪。

---

#### 1.1.8 滚动时域控制（Receding Horizon Control）

Diffusion Policy 不仅是一个生成模型，还是一个闭环控制策略。其推理流程如下：

1. 获取当前观测 $\mathbf{O}_t$（最近的 2 帧图像 + agent 位置）
2. 从随机噪声开始，经过 K 步去噪（DDPM 或 DDIM），生成动作序列 $\mathbf{A}_t \in \mathbb{R}^{16 \times 2}$（16 步，每步 2 维 XY 坐标）
3. 只执行前 $N_{\text{exec}}$ 步动作（DP 中通常 $N_{\text{exec}} = 8$）
4. 等待环境返回新的观测 $\mathbf{O}_{t+N_{\text{exec}}}$
5. 重复步骤 1-4

这种滚动时域设计的优点：
- **闭环反馈**：每次重新规划都使用了最新的环境观测，可以纠正累积误差
- **在线适应**：如果执行过程中出现意外干扰（如 agent 滑了一下），下一次规划会自动补偿
- **时间一致性**：扩散模型生成的动作序列本身具有时序平滑性（前一步和后一步相关），避免了逐帧预测的抖动

**时间步对齐**：DP 使用了一种精妙的序列索引约定。在时间轴上：
```
obs: [t-1, t]  →  预测: action[t, t+1, ..., t+15]  →  执行: action[t, t+1, ..., t+7]
```
即观测窗口为 2 帧，预测窗口为 16 帧（含观测的第 2 帧时刻），执行窗口为前 8 帧。下一次观测时恰好落在 t+8 的位置，与上次预测窗口有 8 帧重叠。这种重叠设计增加了控制的平滑性。

---

#### 1.1.9 PushT 任务中的具体配置

**PushT 任务**是 DP 论文中的旗舰实验。这是一个模拟推块任务：
- 桌面上有一个 T 形块和一个圆形 agent
- Agent 推动 T 块使其到达目标位置（绿色半透明目标标记）
- 输入是 96×96 的俯视相机图像
- 输出是 agent 在 XY 平面上的目标位置（2 维动作，范围 [0, 512]）
- 评估指标是"覆盖率"——T 块覆盖目标区域的比例，最大值约 1.0

在本项目中的 DP 配置参数：

| 参数 | 值 | 说明 |
|------|-----|------|
| 观测窗口 $T_o$ | 2 | 取最近 2 帧 |
| 动作预测长度 $T_a$ | 16 | 预测未来 16 步 |
| 执行长度 $N_{\text{exec}}$ | 8 | 每次执行前 8 步 |
| 扩散步数 T | 100（训练用 DDPM），10（推理用 DDIM） | |
| 动作维度 | 2 | XY 坐标 |
| 噪声调度 | squaredcos_cap_v2（余弦调度变种） | |
| 视觉编码器 | ResNet18（无预训练，GroupNorm 替代 BatchNorm，spatial softmax） | DP 论文 3.2 节原文 |
| 扩散骨干 | ConditionalUnet1D（1D 卷积 UNet） | 论文同时提供 Transformer 版本 |
| 条件注入方式 | FiLM（CNN 版本）/ Cross-Attention（Transformer 版本） | |
| 参数量 | ~287M（详见 1.1.10 节手算） | UNet ~276M + ResNet18 ~11M |
| 归一化 | MinMax 归一化到 [-1, 1] | |

官方 DP 在 3050 个 epoch（约 500k 步）训练后，可达 **0.949** 的分数。本项目自训练的 DP（50 epoch）达到 0.726，作为本地复现基线。

**关键数据**：PushT 完整数据集包含 **206 条专家演示轨迹**（DP 论文标准为 200 条），DP 官方配置取前 90 条作为训练集（`max_train_episodes: 90`），每条约 125 步。本项目沿用此配置，即在 90 条轨迹（约 11,250 个 (观测, 动作) 对）上进行训练。这是一个典型的小样本模仿学习场景——也是为什么后来 B3（5M 参数）和 E4（5M 参数）能以远小于 UWM（150-305M）的参数量达到更高分数：**在数据极少的情况下，正确的归纳偏置（条件化机制）比参数量重要得多。**

### 1.2 Unified World Model（统一世界模型）—— 架构详解

Unified World Model（简称 UWM）是一个更宏大的框架。不同于 DP 只做动作生成，UWM 的核心设计理念是 **"世界模型"** —— 通过统一的联合扩散框架，同时支持以下四种能力（通过控制动作时间步 $t_a$ 和视频时间步 $t_{o'}$ 实现）：

1. **策略/动作预测（Policy）** —— $p(\mathbf{a} \mid \mathbf{o})$：给定当前观测，生成动作序列。实现方式：设置 $t_{o'} = T$（将未来视频边缘化/掩盖），仅对动作去噪。
2. **前向动力学（Forward Dynamics）** —— $p(\mathbf{o}' \mid \mathbf{o}, \mathbf{a})$：给定当前观测和干净动作，预测未来视频帧。实现方式：设置 $t_a = 0$（将动作作为干净条件注入），仅对视频去噪。
3. **逆动力学（Inverse Dynamics）** —— $p(\mathbf{a} \mid \mathbf{o}, \mathbf{o}')$：给定当前观测和未来观测，推断中间动作。实现方式：设置 $t_{o'} = 0$（将未来视频作为干净条件注入），仅对动作去噪。
4. **视频预测（Video Prediction）** —— $p(\mathbf{o}' \mid \mathbf{o})$：在无动作显式引导下，单纯预测未来视频帧。实现方式：设置 $t_a \approx T$（将动作边缘化/掩盖），仅对视频去噪。此能力使 UWM 可以利用无动作标注的视频数据进行协同训练（co-training）。UWM 论文实验主要验证了机器人视频/去动作标签数据的作用；互联网人类视频到机器人控制的 embodiment gap 仍属于更远期扩展方向，论文并未证明其已有效利用大规模互联网人类视频。

这四种能力对应了 UWM 的四种推理模式（后文详述）。

UWM 的总参数量约 305M（3 亿），属于大模型路线。它的设计目标是面向 DROID 等大规模多任务机器人数据进行预训练，再微调到具体任务。原论文真实机器人实验中使用了 DROID 子集（2000 条 action-labeled 轨迹预训练 + 额外 2000 条 action-free 轨迹 co-training），并非百万级轨迹。但本项目研究的是：**在只有 90 条 PushT 训练轨迹的极小数据、从随机初始化直接训练的情况下，UWM 能做到什么程度？瓶颈是什么？**

---

#### 1.2.1 整体架构总览

UWM 的数据流可分解为以下步骤：

```
输入观测 obs_dict                 目标动作 action              目标视频 next_obs
     │                                │                            │
     ▼                                ▼                            ▼
┌──────────────┐              ┌──────────────┐           ┌──────────────┐
│ 观测编码器     │              │ 动作编码器     │           │ VAE 压缩      │
│ ObsEncoder   │              │ action_enc   │           │ + Patchifier │
│              │              │ (MLP)        │           │ (Conv3D)     │
└──────┬───────┘              └──────┬───────┘           └──────┬───────┘
       │                             │                          │
       ▼                             ▼                          ▼
  global_cond [B,1540]        action_tokens [B,16,768]    video_tokens [B,N,768]
  (AdaLN) 或                                               (联合模式)
  obs_memory [B,20,768]                                         │
  (cross-attention)                                              │
       │                             │                          │
       └─────────┬───────────────────┘                          │
                 │                                              │
                 ▼                                              ▼
          ┌──────────────────────────────────────────────────────┐
          │               Token 序列拼接 + 位置编码               │
          │   [action_tokens | video_tokens | register_tokens]   │
          │         16       |     N        |       8           │
          └──────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
          ┌──────────────────────────────────────────────────────┐
          │         12 层 AdaLN-Attention / Hybrid Block         │
          │    (条件来自 global_cond + timestep_embed)            │
          └──────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
          ┌──────────────────────────────────────────────────────┐
          │                   AdaLNFinalLayer                    │
          └──────────────────────┬───────────────────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    ▼                         ▼
          ┌─────────────────┐       ┌─────────────────┐
          │ action_decoder  │       │ unpatchify      │
          │ (MLP)           │       │ (Linear+rearr)  │
          └────────┬────────┘       └────────┬────────┘
                   ▼                         ▼
          动作噪声预测 [B,16,2]      视频噪声预测 [B,V,C,T,H,W]
```

核心设计要点：
- **单一 Transformer 骨干同时处理动作和视频 token**，两者通过 self-attention 交互
- 条件化不是拼接 token，而是通过 AdaLN 的 scale/shift/gate 调制注入（或 cross-attention 替代）
- **双时间步嵌入**：动作和视频各自有独立的扩散时间步，可以处于不同的噪声水平

---

#### 1.2.2 观测编码器（UWMObservationEncoder）

这是 UWM 的第一个核心组件，负责将原始观测编码为条件信号。支持两种编码模式：

**模式 A：全局池化编码（用于 AdaLN 条件化）** —— `encode_curr_obs()`

```
图像 [B, 2, 96, 96, 3]
   → VideoTransform (数据增强: 随机裁剪、颜色抖动)
   → ResNet/ViT 编码器 → 图像特征 [B, V×T×768]
   → 与 agent_pos 展平拼接 [B, V×T×768 + 2×2] = [B, 1540]
   = global_cond  (单一全局向量，用于 AdaLN 调制)
```

注意：此处编码的是一个**池化后的全局向量**，维度仅 1540。所有空间信息被压缩到这一个向量中。这是 AdaLN 条件化的核心信息瓶颈。

**模式 B：Token 级编码（用于 Cross-Attention 条件化）** —— `encode_obs_memory()`

```
图像 [B, 2, 96, 96, 3]
   → VideoTransform
   → ResNet 的 encode_tokens() → 空间网格特征 [B, 18, 768]
        (每帧 3×3 = 9 个空间 token，2 帧共 18 个)
   → 与 agent_pos token 拼接 [B, 20, 768]
        (每帧 1 个 agent_pos token，2 帧共 2 个)
   = obs_memory  (20 个 token，作为 cross-attention 的 memory)
```

关键区别：模式 A 将观测压缩为向量用于全局调制，模式 B 保留为 token 序列供 action token 逐个查询。这是本项目中 'adaln' 和 'cross_attn' 两种条件化类型的根本差异所在。

**VAE 压缩（用于视频预测）** —— `encode_next_obs()`

当需要预测未来视频时（非 dp-only 模式），未来帧通过 SDXL-VAE 压缩到潜在空间：

```
未来帧 [B, 1, 3, 2, 96, 96]  (1 视角, 3 通道, 2 帧, 96×96)
   → VideoTransform (与当前观测共享相同增强)
   → SDXL-VAE 编码 → latent [B, 1, 4, 2, 12, 12]  (4 通道, 下采样 8×)
```

VAE 将 96×96 图像压缩为 12×12 的潜在表示，每个像素变为 4 维的 latent 向量，大幅减少了后续 Transformer 需要处理的 token 数量。

---

#### 1.2.3 Token 化：动作编码器与视频 Patchifier

进入 Transformer 之前，所有数据都需要转换为相同维度（embed_dim = 768）的 token。

**动作编码器** —— 简单的两层 MLP：

$$
\mathbf{a}^{\text{tok}} = \text{MLP}_{\text{action}}(\mathbf{a}) \in \mathbb{R}^{B \times 16 \times 768}
$$

其中 $\mathbf{a} \in \mathbb{R}^{B \times 16 \times 2}$ 是加了噪声的动作序列。MLP 结构为 Linear(2→3072) → Mish → Linear(3072→768)。

**视频 Patchifier** —— 3D 卷积切块：

$$
\mathbf{v}^{\text{tok}} = \text{Conv3D}(\mathbf{v}^{\text{latent}}) \in \mathbb{R}^{B \times N_v \times 768}
$$

其中 $\mathbf{v}^{\text{latent}} \in \mathbb{R}^{B \times 1 \times 4 \times 2 \times 12 \times 12}$（VAE 潜在表示），Conv3D 的 kernel 和 stride 均为 (2, 4, 4)，即每个 patch 覆盖时间维度上的 2 帧、空间维度上的 4×4 区域。对于 PushT 的 12×12 latent map：

$$
N_v = 1 \times \lfloor 2/2 \rfloor \times \lfloor 12/4 \rfloor \times \lfloor 12/4 \rfloor = 1 \times 1 \times 3 \times 3 = 9 \text{ 个视频 token}
$$

> **注：分辨率适配。** UWM 原论文默认使用 224×224 输入（先 resize 到 240×320 再随机裁剪到 224×224）。224×224 经 SDXL-VAE 8 倍下采样后得到 28×28 的 latent map，再经同样的 patch(2,4,4) 切分后产生 1×7×7 = **49 个视频 token**（每视角）。本项目为适配 PushT 任务的原生 96×96 渲染分辨率，跳过了 resize/crop 步骤，直接使用 96×96 输入，VAE 压缩后得到 12×12 latent map，patch 化后仅产生 **9 个视频 token**——相比原论文减少了 82%。这不仅降低了 Transformer 的序列长度，也意味着视频预测任务的空间精度要求更低，信息密度更稀疏。这一适配是合理的（PushT 是简笔画风格的俯视渲染，不需要 224×224 的高分辨率），但也意味着 UWM 的视频预测能力在 PushT 上未经充分考验。

加上 8 个可学习的 register token（用于存储全局信息），最终的 token 序列为：

```
[动作 token: 16 | 视频 token: 9 | register token: 8] = 总共 33 个 token (dp-only 则仅 24 个)
```

每个 token 维度为 768。所有 token 加上可学习的位置编码后进入 Transformer 层。

---

#### 1.2.4 双时间步编码（DualTimestepEncoder）

UWM 的一个独特设计是**动作和视频各有一个独立的扩散时间步**：

$$
t_{\text{act}}, t_{\text{video}} \in [0, T-1]
$$

两个时间步分别通过正弦位置编码映射为 embed_dim 维的向量，然后拼接后通过 MLP 融合：

$$
\mathbf{e}_{\text{time}} = \text{MLP}\big([\text{SinusoidalEmb}(t_{\text{act}}); \text{SinusoidalEmb}(t_{\text{video}})]\big) \in \mathbb{R}^{B \times 512}
$$

这个设计的灵活性在于：
- 训练时，可以独立采样两个时间步，实现不同的噪声调度组合
- 推理时，可以让动作处于高噪声水平（需要大量去噪）而视频处于低噪声水平（只需要微调），或反之
- 在 `sample_marginal_action()` 中，视频时间步固定在最大值（纯噪声），动作时间步正常迭代去噪

---

#### 1.2.5 核心：AdaLN Attention Block

这是 UWM 架构的心脏。每个 block 接收两类输入：
- **x**：token 序列 [B, seq_len, 768]
- **cond**：条件向量 [B, cond_dim]，其中 cond_dim = global_cond_dim + timestep_embed_dim（AdaLN模式）或仅 timestep_embed_dim（cross-attention 模式）

**AdaLN 的数学定义**：

Block 内的操作分为 attention 子层和 MLP 子层。对于每个子层，AdaLN 调制首先从条件向量 cond 预测 6 个调制参数：

$$
[\gamma_1, \beta_1, \alpha_1,\; \gamma_2, \beta_2, \alpha_2] = \text{SiLU}(\text{Linear}(\mathbf{c}_{\text{cond}})) \in \mathbb{R}^{6 \times 768}
$$

然后对 token 序列进行调制：

**子层 1（自注意力）**：
$$
\begin{aligned}
\mathbf{h} &= \text{LayerNorm}(\mathbf{x}) \quad \text{(无参数 LN，不做仿射变换)} \\
\mathbf{h}' &= (1 + \gamma_1) \odot \mathbf{h} + \beta_1 \quad \text{(scale + shift 调制)} \\
\mathbf{h}'' &= \text{SelfAttention}(\mathbf{h}') \\
\mathbf{x} &= \mathbf{x} + \alpha_1 \odot \mathbf{h}'' \quad \text{(gate 门控)}
\end{aligned}
$$

**子层 2（MLP）**：
$$
\begin{aligned}
\mathbf{h} &= \text{LayerNorm}(\mathbf{x}) \\
\mathbf{h}' &= (1 + \gamma_2) \odot \mathbf{h} + \beta_2 \\
\mathbf{h}'' &= \text{MLP}(\mathbf{h}') \\
\mathbf{x} &= \mathbf{x} + \alpha_2 \odot \mathbf{h}'' 
\end{aligned}
$$

综合起来，一个 AdaLNAttentionBlock 的完整变换为：

$$
\boxed{
\begin{aligned}
\mathbf{x} &= \mathbf{x} + \alpha_1 \odot \text{Attn}\big((1+\gamma_1) \odot \text{LN}(\mathbf{x}) + \beta_1\big) \\
\mathbf{x} &= \mathbf{x} + \alpha_2 \odot \text{MLP}\big((1+\gamma_2) \odot \text{LN}(\mathbf{x}) + \beta_2\big)
\end{aligned}
}
$$

其中所有 6 个调制参数 $(\gamma_1,\beta_1,\alpha_1,\gamma_2,\beta_2,\alpha_2)$ 都从条件向量 cond 通过一个线性层预测得到。

**AdaLNHybridAttentionBlock（cross-attention 变体）**：

在 self-attention 和 MLP 之间插入一个 cross-attention 子层：

$$
\begin{aligned}
\mathbf{x} &= \mathbf{x} + \alpha_1 \odot \text{SelfAttn}\big((1+\gamma_1) \odot \text{LN}(\mathbf{x}) + \beta_1\big) \\
\mathbf{x} &= \mathbf{x} + \text{CrossAttn}\big(\text{LN}(\mathbf{x}),\; \mathbf{c}_{\text{obs}}\big) \quad \text{(无 AdaLN，直接查询 obs memory)} \\
\mathbf{x} &= \mathbf{x} + \alpha_2 \odot \text{MLP}\big((1+\gamma_2) \odot \text{LN}(\mathbf{x}) + \beta_2\big)
\end{aligned}
$$

注意：cross-attention 子层**不使用 AdaLN 调制**（仅对 self-attention 和 MLP 使用），且 cond 维度从 global_cond_dim + timestep_embed_dim 缩减为仅 timestep_embed_dim（因为观测信息通过 cross-attention 直接注入，无需进 AdaLN）。

**权重初始化细节**：所有 AdaLN 调制层的最后一层 Linear 的权重和偏置被初始化为零。训练开始时 $(\gamma,\beta,\alpha)=(0,0,0)$：
- $\gamma=0,\beta=0$ → scale 和 shift 不改变归一化特征（`modulate` 退化为恒等）；
- $\alpha=0$（gate 门控为零）→ attention 和 MLP 残差分支的输出被完全置零；
- **因此 block 初始近似为恒等映射（identity）**，而非普通 attention + MLP 残差块。

这种 **AdaLN-Zero** 风格初始化源自 DiT 论文（Peebles & Xie, 2023），有助于稳定深层 Transformer 的训练，让条件信号在训练过程中逐步"开启"。

---

#### 1.2.6 最终输出层（AdaLNFinalLayer）

经过 12 层 Block 处理后，token 序列通过一个最终的 AdaLN 层：

$$
\begin{aligned}
\mathbf{h} &= \text{LayerNorm}(\mathbf{x}) \\
\mathbf{h}' &= (1+\gamma) \odot \mathbf{h} + \beta \\
\mathbf{x}^{\text{out}} &= \text{Linear}(\mathbf{h}')
\end{aligned}
$$

然后从输出序列中分别取出对应位置：
- `x[:, 0:16, :]` → `action_decoder` → 动作噪声预测 $\hat{\boldsymbol{\epsilon}}_{\text{act}} \in \mathbb{R}^{B \times 16 \times 2}$
- `x[:, 16:25, :]` → `unpatchify` → 视频噪声预测 $\hat{\boldsymbol{\epsilon}}_{\text{video}} \in \mathbb{R}^{B \times 1 \times 4 \times 2 \times 12 \times 12}$

---

#### 1.2.7 训练损失：双目标优化

UWM 的训练 loss 包含两项：

$$
\boxed{
\mathcal{L} = \underbrace{\|\hat{\boldsymbol{\epsilon}}_{\text{act}} - \boldsymbol{\epsilon}_{\text{act}}\|_2^2}_{\text{动作扩散损失 } \mathcal{L}_{\text{action}}} \;+\; \lambda \cdot \underbrace{\|\hat{\boldsymbol{\epsilon}}_{\text{video}} - \boldsymbol{\epsilon}_{\text{video}}\|_2^2}_{\text{视频扩散损失 } \mathcal{L}_{\text{dynamics}}}
}
$$

其中 $\lambda$ 是视频 loss 的权重（`dynamics_loss_weight`），默认值 1.0：

- $\lambda = 1.0$：标准联合训练，两个 loss 等权重
- $\lambda = 0.0$：等价于只训练动作（R1 实验），但注意与 dp-only 不同——dp-only 完全去掉了视频 token 和 VAE 编码，而 λ=0 仅去掉了视频 loss，视频 token 仍在序列中
- $\lambda = 0.05$：本项目的核心发现——小视频 loss 起正则化作用

训练时，动作和视频的时间步各自独立随机采样：

$$
t_{\text{act}} \sim \text{Uniform}(0, T-1), \quad t_{\text{video}} \sim \text{Uniform}(0, T-1)
$$

---

#### 1.2.8 UWM 论文的四种标准推理模式

UWM 论文提出通过统一联合扩散框架支持四种推理模式。**核心机制是通过控制动作时间步 $t_a$ 和视频时间步 $t_{o'}$ 来切换条件化/边缘化行为**：

- **时间步 = 0**：变量为干净数据 → 用于**条件化**（conditioning）
- **时间步 ≈ T**：变量接近纯噪声 → 用于**边缘化/掩盖**（marginalization）

DDIM scheduler 的 `timesteps` 为降序排列（如 [90, 80, ..., 10, 0]），因此：
- `timesteps[0]` = 最大值 ≈ T → 边缘化
- `timesteps[-1]` = 0 → 条件化（干净信号）

四种标准模式都是**"条件化一个变量，去噪另一个变量"**——两个时间步永远不会同时迭代：

| 模式 | 数学形式 | $t_a$ | $t_{o'}$ | 采样方法 |
|------|------|:---:|:---:|------|
| Policy | $p(a \mid o)$ | 迭代去噪 | =T（边缘化视频） | `sample_marginal_action()` |
| Forward Dynamics | $p(o' \mid o, a)$ | =0（条件化动作） | 迭代去噪 | `sample_forward_dynamics()` |
| Inverse Dynamics | $p(a \mid o, o')$ | 迭代去噪 | =0（条件化视频） | `sample_inverse_dynamics()` |
| Video Prediction | $p(o' \mid o)$ | =T（边缘化动作） | 迭代去噪 | `sample_marginal_next_obs()` |

**模式一：策略/动作采样（Policy）** —— `sample_marginal_action()`

对应 $p(\mathbf{a} \mid \mathbf{o})$。"给定观测，生成动作序列"。这是最主要的策略推理模式，也是本项目评估的核心。

```
输入: obs_dict (当前观测)
输出: action [B, 16, 2]

1. 编码观测 → obs_memory
2. action_sample = randn(B, 16, 2)      ← 动作从纯噪声开始
3. next_obs_t = timesteps[0] ≈ T        ← 视频时间步固定为最大值
4. next_obs_sample = randn(...)          ← 视频随机初始化，保持高噪
5. 迭代: for t in timesteps (T→0):
     noise_pred_net(action_sample, t, next_obs_sample, next_obs_t≈T)
     action_sample = scheduler.step(...)
     # next_obs_t 始终 ≈T，视频永不更新 → 边缘化 p(a|o) = ∫ p(a,o'|o) do'
```

> **说明**：视频保持在高噪声是 UWM 论文的**设计意图**——通过边缘化实现 $p(a|o)$。这不是代码 bug。

> **本项目发现**：dp-only（完全去掉视频 token，序列从 33→24）分数（0.491）高于 joint 的 `sample_marginal_action`（0.354），Δ=-0.136（p=0.011）。在小数据 PushT 下，即使是边缘化状态的高噪声视频 token 也可能通过 self-attention 干扰动作 token。UWM 原论文在其大规模多任务设置中未报告此现象。

**模式二：前向动力学（Forward Dynamics）** —— `sample_forward_dynamics()`

对应 $p(\mathbf{o}' \mid \mathbf{o}, \mathbf{a})$。"给定观测和干净动作，预测未来视频"。

```
action_t = timesteps[-1] = 0           ← 动作=0，作为干净条件注入
next_obs 从纯噪声开始，迭代去噪
```

**模式三：逆动力学（Inverse Dynamics）** —— `sample_inverse_dynamics()`

对应 $p(\mathbf{a} \mid \mathbf{o}, \mathbf{o}')$。"给定观测和未来观测，推断中间动作"。

```
next_obs_t = timesteps[-1] = 0         ← 视频=0，作为干净条件注入
action 从纯噪声开始，迭代去噪
```

**模式四：视频预测（Video Prediction）** —— `sample_marginal_next_obs()`

对应 $p(\mathbf{o}' \mid \mathbf{o})$。"只给观测，预测未来视频"。

```
action_t = timesteps[0] ≈ T            ← 动作≈T，边缘化
next_obs 从纯噪声开始，迭代去噪
```

这四种模式共同构成了 UWM 的**统一世界模型**：同一个模型，通过控制两个时间步的取值，在四种能力之间自由切换，无需重新训练。

---

#### 1.2.9 UWM 在 PushT 上的具体配置

> **注意**：以下为本项目将 UWM 改造到 PushT 上的实现设置，**不是 UWM 原论文的默认配置**。UWM 原论文默认图像尺寸为 224×224（见附录 Table V），主要实验在 DROID（真实机器人）和 LIBERO（仿真）等数据集上进行，未包含 PushT 任务。

| 参数 | 值 | 说明 |
|------|-----|------|
| Transformer 层数 | 12 | |
| 嵌入维度 | 768 | |
| 注意力头数 | 12 | |
| MLP 扩展比 | 4.0 | 即 MLP 隐层 = 768 × 4 = 3072 |
| Register token 数 | 8 | 可学习的全局存储 token |
| 动作预测长度 | 16 | |
| 动作维度 | 2 | XY 坐标 |
| 观测帧数 | 2 | 取最近 2 帧 |
| 视频预测帧数 | 2 | |
| 图像尺寸 | 96×96 | UWM 原生默认 224×224（resize→crop），本项目为适配 PushT 原生渲染分辨率跳过 resize/crop |
| VAE 压缩后尺寸 | 12×12 | SDXL-VAE 8 倍下采样；原生 224×224 时为 28×28 |
| 视频 token 数（每视角） | 9 | 原生 224×224 时为 49；减少了 82% |
| Vision backbone | ResNet | ImageNet 预训练初始化（`weights="IMAGENET1K_V1"`），训练中继续更新 |
| 扩散训练步数 | 100 | squaredcos_cap_v2 调度 |
| 扩散推理步数 | 10 | DDIM |
| 总参数量（joint） | ~306M | |
| 总参数量（dp-only） | ~306M | 去掉了 unpatchify 解码器 |

---

#### 1.2.10 UWM 与 DP 在 PushT 上的架构对比

| 维度 | Diffusion Policy | UWM (AdaLN-DiT) | UWM (Cross-Attn) |
|------|:---:|:---:|:---:|
| 扩散骨干 | 1D 卷积 UNet | 12 层 AdaLN Transformer | 12 层 Hybrid Transformer |
| 条件注入 | **FiLM** (逐通道 scale+shift) | **AdaLN** (逐 token 统一 scale+shift+gate) | **Cross-Attention** (token 查询 obs memory) |
| 条件编码 | 全局池化向量 | 全局池化向量 (1540D) | Token 序列 (20×768D) |
| 自注意力 | 无 (1D 卷积替代) | 双向 | 双向 |
| 时序建模 | UNet 多尺度卷积核 | 位置编码 + self-attention | 位置编码 + self-attention |
| 视频预测 | 无 | 有 (joint) | 有 (joint) |
| 双时间步 | 无 (仅动作) | 有 (动作+视频独立) | 有 |
| 视觉编码器 | ResNet18 (无预训练，GroupNorm) | ResNet/ViT (ImageNet 预训练初始化) | ResNet (ImageNet 预训练初始化) |
| 参数量 | ~287M | 150-306M | 247-306M |
| 归一化 | GroupNorm | LayerNorm (无参数) | LayerNorm (无参数) |
| 推理步数 | 100 DDPM / 10 DDIM (任务而异) | 10 DDIM | 10 DDIM |
| EMA | 0.9999 | 无 (本项目实验中加入几乎无效) | 无 |
| LR 调度 | Cosine + Warmup | 常数 1e-4 | 常数 1e-4 |
| 训练数据量 | 206 条完整 / 90 条训练 | 90 条训练轨迹 (本项目配置) | 90 条训练轨迹 (本项目配置) |

> **上表说明**：DP 列中的视觉编码器配置来自 DP 论文原文（ResNet-18 without pretraining, GroupNorm 替换 BatchNorm）；UWM 列中的配置来自 UWM 论文附录（ImageNet 预训练初始化并全程更新）。推理步数列：DP 在不同任务中混用 DDPM/DDIM，并非一律 100 步。训练数据量列：DP 的 PushT 完整数据集 206 条，train split 取 90 条；UWM 两列的 90 条是沿用此训练集配置的本项目实现。

**关键差异的直觉理解**：

1. **UNet 卷积 vs Transformer 注意力**：UNet 的多尺度卷积天然具有局部感受野和层次化特征提取能力，这对 PushT 这种空间精度任务很友好。Transformer 的全局自注意力虽然理论上更灵活，但在数据极少时难以学到有效的注意力模式。

2. **FiLM vs AdaLN vs Cross-Attention**：这是三个层次的条件注入方式——
   - FiLM：逐通道的条件调制（最细粒度）
   - AdaLN：逐 token 的条件调制（粗粒度，所有 token 一致）
   - Cross-Attention：让 token 自主选择条件信息

   > **注意**：直觉上"Cross-Attention 最灵活"是本项目的假设。UWM 论文附录中的消融实验显示，在其 LIBERO 多任务设置中，cross-attention UWM **反而弱于** AdaLN UWM——论文将此归因于动作预测任务可能更受益于全局调制而非 per-token 局部调制。本项目在 PushT 小数据上的发现（cross-attention 优于 AdaLN）与 UWM 论文的消融结论相反，说明该结论可能具有任务/数据规模依赖性。

3. **全局池化 vs Token 序列**：DP 和 AdaLN-UWM 都将观测压缩为单一全局向量，但 DP 的 UNet 通过多尺度卷积隐式地保留了空间结构。AdaLN-DiT 中，全局向量通过 scale/shift 调制注入，**观测中的显式空间 token 信息被压缩**，在 PushT 这类依赖精细几何位置的任务中可能形成信息瓶颈。Cross-Attention 通过保留 token 序列缓解了这个问题（但 UWM 论文消融显示此改进并非通用优势）。

4. **最关键的结构差异**：DP 的 FiLM 条件化作用于 UNet 的每一层、每个通道——它给了条件信息充分的表达空间。而 UWM 的 AdaLN 将 96×96 的图像信息压缩到 1540 维的全局条件向量，再在每一层中转化为若干组逐通道的 scale/shift/gate 调制向量。虽然这些调制是逐通道的（每个通道独立 scale/shift），但它们对同一层内所有 token 共享——即所有 16 个动作 token 接收完全相同的调制参数，显式空间 token 信息无法像 cross-attention 那样被不同动作 token 单独查询。信息被层层压缩，**形成了严重的条件信息瓶颈**。这正是本项目最终定位的核心问题。

---

> **本文边界声明**：以下内容涉及三类不同性质的信息，请读者注意区分：
>
> 1. **论文原始结论**：来自 DDPM (Ho et al., 2020)、Diffusion Policy (Chi et al., 2023)、UWM 论文的方法描述与实验结论；
> 2. **本项目实现配置**：如 PushT 96×96 输入、90 条训练轨迹、10-step DDIM、UWM 的 dp-only / cross-attention 改造等，这些是本项目将 UWM 迁移到 PushT 上的具体工程选择，不代表原论文默认配置；
> 3. **本项目实验发现**：如 AdaLN 瓶颈假说、video loss 梯度冲突、λ=0.05 正则化效应、cross-attention 在小数据 PushT 上优于 AdaLN 等，这些是本项目在特定条件下得到的诊断结论，不应被理解为原论文已经报告的结果。
>
> 后两类内容在正文中将尽量以"本项目发现""本项目实验中"等措辞标识。

---

## 第二章：起点——为什么 UWM 在 PushT 上表现这么差？

### 2.1 第一次训练：0.11

将 PushT 数据集接入 UWM 框架，从零训练 20k 步，评估 50 个回合。

**结果：0.11**（满分约 1.0）

而同一台机器上自训练的 DP 只需要 50 个 epoch（约 8,400 步）就达到了 **0.73**。

差距是 6.6 倍。

### 2.2 最初的怀疑列表

面对这个结果，可能的解释排成了一个列表：

1. **工程 Bug**——是不是 normalizer 有问题？是不是数据格式不对？
2. **视觉编码器太弱**——虽然 UWM 的 ResNet 使用了 ImageNet 预训练权重初始化，但 PushT 的 96×96 简笔画风格渲染与 ImageNet 自然图像分布差异大，预训练优势可能有限。是不是视觉特征提取不够好？
3. **模型太大/太小**——305M 参数对 90 条轨迹是不是严重过参数化了？
4. **视频预测头拖后腿**——同时预测动作和视频，两个 loss 互相打架？
5. **数据或评估有问题**——数据集本身或者评估脚本有 bug？
6. **条件化机制不合适**——AdaLN 这个全局调制是不是对 PushT 这种空间精确任务不够？
7. **训练食谱差异**——没有 EMA、没有 cosine LR、推理步数太少？

**核心思路**：不猜测，逐一做实验排除。每一个怀疑都用一个受控实验来验证。

---

## 第三章：工程修复——从 0.11 到 0.43

### 3.1 Bug 1：agent_pos 归一化不一致

训练时，`PushTDataset` 悄无声息地将 `agent_pos` 从 [0, 512] 归一化到 [-1, 1]。但评估脚本直接传入了原始坐标。

这意味着：训练时模型学到的 agent_pos 分布是 [-1, 1]，评估时看到的却是 [0, 512]，范围差了 250 倍。

**修复**：评估脚本加入和训练一致的归一化。

**效果**：0.11 → 0.19

### 3.2 Bug 2：clip_sample 截断

DP 官方使用 MinMax 归一化（动作自然落在 [-1, 1] 内），`clip_sample=True` 不会造成截断。

但 UWM 使用了均值和标准差归一化，归一化后的动作范围是 [-2.1, 2.8]。扩散推理时，`clip_sample=True` 会根据 scheduler 的配置将去噪结果裁剪到 [-1, 1]，直接切掉了约 40% 的有效动作范围。

**修复**：将 `clip_sample` 设为 False。

**效果**：0.19 → 0.43

### 3.3 修复后的小结

经过两个工程修复，UWM 的分数从 0.11 跃升到约 0.43。

这意味着：**最初 0.11 的低分主要是工程问题，不能用于评价 UWM 方法本身。**

但修复后的 0.43 仍然远低于 DP 官方的 0.95。差距还有 0.52。

真正的诊断从这里开始。

---

## 第四章：系统排查——逐一排除可疑因素

### 4.1 视频预测头：是拖累吗？

**实验设计**：用同一个 AdaLN-DiT 骨干训练两个模型——
- UWM joint（动作+视频联合训练）
- DP-only（只做动作扩散，去掉整个视频头）

**结果**：
| 模型 | 分数 |
|------|:---:|
| UWM joint C | 0.434 |
| DP-only C | 0.391 |

**差距仅 0.043，统计上不显著（配对 t 检验 p=0.40）**。

**结论**：在当前 AdaLN 条件下，视频预测头既没有明显帮助也没有明显拖累。去掉视频头不是解药。

### 4.2 视觉编码器：是瓶颈吗？

**实验设计**：把输入从"96×96 图像"换成"20 维关键点向量"（9 个关键点 × 2 坐标 + agent 的 2 维坐标）。关键点已经精确编码了 T 块的空间位置和朝向，不需要任何视觉编码器去提取特征。

如果视觉编码器是瓶颈，换了关键点后分数应该大幅上升。

**结果**：
| 模型 | 输入 | 分数 |
|------|------|:---:|
| M4 joint C | 图像 | 0.434 |
| M7 UWM-DP-KP | 20 维关键点 | 0.442 |

**差距仅 0.008，几乎没有变化**。

**结论**：即使给了更干净的关键点信息，AdaLN-DiT 仍然停留在 0.44。**视觉编码器不是当前主要瓶颈。** 瓶颈在更下游的地方。

### 4.3 模型规模：大了还是小了？

**实验设计**：将 AdaLN-DiT 从 12 层/768 维/150M 参数缩小到 6 层/256 维/11M 参数（缩小 14 倍）。

**思路**：如果模型太大、过度参数化导致在小数据上过拟合，缩小模型应该反而更好。

**结果**：
| 模型 | 参数量 | 分数 |
|------|:---:|:---:|
| M7 UWM-DP-KP | 150M | 0.442 |
| M8 UWM-DP-KP small | 11M | 0.389 |

缩小 14 倍，分数下降了 0.05。不是过参数化的问题。

**结论**：模型规模不是瓶颈。150M 相比 11M 只提升了 0.05，参数效率极低（每百万参数只产出 0.003 分）。

### 4.4 EMA 和 LR 调度的实验

给 AdaLN-DiT 添加 EMA（指数移动平均，衰减率 0.9999），从已收敛的 checkpoint 继续训练 500 步。

**结果**：EMA 带来 +0.006 的微弱提升（0.391 → 0.397）。

**结论**：EMA 几乎不产生作用。cosine LR 和 warmup 的完整实验也未完成（非最高优先级）。

**训练食谱不是主要差距来源。**

---

## 第五章：破局关键——外部正对照实验

### 5.1 核心思路

此时所有 UWM 内部实验都围绕 0.39-0.44 徘徊。一个重要问题悬而未决：

**"到底是数据/eval 协议有问题，还是 UWM 的架构有问题？"**

如果是数据有 bug 或者评估流程不合理，那么任何模型在这个数据上都不应该高分。反之，如果有一个完全不同的模型能在这个数据上拿高分，就说明数据和评估没问题，问题出在 UWM 架构。

**于是引入了"外部正对照"**——完全不使用 UWM 代码的独立模型，只使用同一份 PushT 数据和同一套评估协议。

### 5.2 B3：手写小型 Transformer（obs-as-token）

用 PyTorch 原生 `TransformerEncoder` 写了一个微型模型：
- 6 层，256 维，8 个注意力头
- 参数仅 5M（比 UWM 小 60 倍）
- 观测（20 维关键点）作为一个 token 直接拼入 self-attention 序列
- 无 EMA，无 cosine LR，常数学习率
- 训练 20k 步

**结果：0.722**

**这意味着什么？** 一个仅 5M 参数、没有复杂设计的简单 Transformer，在相同数据上达到了 UWM 150M 参数的近两倍分数。数据没问题，评估也没问题。问题锁定在 UWM 架构内部。

### 5.3 E4：DP 官方 Transformer（cross-attention）

使用 DP 官方的 `TransformerForDiffusion` 骨干：
- 8 层 TransformerDecoder，256 维，4 个注意力头
- 因果自注意力 + cross-attention 到观测 memory
- 参数 5M
- EMA 0.9999，cosine LR + warmup，100 步 DDPM 推理

**结果：0.998**

**这意味着什么？** 5M 参数可以达到接近满分。而 UWM 的 150M 参数只有 0.44。

**参数规模不是答案。正确的架构才是。**

> **重要声明**：E4（以及 B3）使用 20D 关键点输入（T 块和 agent 的空间坐标已显式给出），而非从 96×96 图像像素中端到端识别。它们本质上是 **privileged state/keypoint 条件**，输入难度低于图像策略。因此 E4/B3 在此的主要作用是验证**数据完整性、评估协议正确性、以及 Transformer + cross-attention 条件化路线的可行性**——它们证明了 0.44 不能归因于数据或评估的 bug。但它们不能作为与图像策略"公平同模态"的性能比较基准。

### 5.4 差异分解

B3（0.722）和 E4（0.998）与 UWM AdaLN-DiT（0.442）之间的关键差异：

| 差异项 | UWM AdaLN-DiT | B3 | E4 |
|--------|:---:|:---:|:---:|
| 条件化机制 | AdaLN 全局调制 | obs 作为 token | cross-attention |
| Transformer 类型 | 自研 AdaLN Block | nn.TransformerEncoder | nn.TransformerDecoder |
| 自注意力 mask | 双向 | 双向 | 因果 |
| EMA | 无 | 无 | 0.9999 |
| LR 调度 | 常数 | 常数 | cosine + warmup |
| 推理步数 | 10 DDIM | 10 DDIM | 100 DDPM |
| 参数量 | 150M | 5M | 5M |
| 分数 | 0.442 | 0.722 | 0.998 |

**最强嫌疑集中在条件化机制上——AdaLN vs cross-attention / obs-as-token。**

---

## 第六章：深入根因——为什么 AdaLN 不适合 PushT

### 6.1 PushT 需要什么

PushT 是一个强空间几何任务。模型需要从观测中精确提取信息：
- T 块的 XY 坐标在哪里？
- T 块朝向什么角度？
- Agent 当前在什么位置？
- 目标在什么位置？

然后为 16 个未来时间步各自生成对应的 [x, y] 目标坐标。

第 1 步可能需要向左大幅度移动靠近 T 块，第 8 步可能只需要微小调整方向。**每个动作 token 需要的信息不同。**

### 6.2 AdaLN 的问题

AdaLN 将全部观测信息压缩为三个全局向量（scale、shift、gate），然后对所有 token 施加完全相同的调制。这意味着：

1. **空间信息被压缩和弱化**：T 块的位置和朝向被混入同一个全局 embedding，模型难以区分"agent 在左还是右"、"T 块朝上还是朝下"这类精确信息。

2. **缺少 token 级差异化**：同一层的 16 个动作 token 接收完全相同的 scale/shift/gate 调制。无法为不同时间步提供不同强度的引导。

3. **信息瓶颈**：从 96×96 图像（9216 维信息）压缩到 768 维全局向量，再压缩到 3 个调制参数，信息损失巨大。

### 6.3 为什么 cross-attention 更好

在 cross-attention 方式中：
- 观测被编码为**多个 token**（如 20 个），保留空间信息
- 每个动作 token 通过 attention 权重**自行选择**从观测中提取什么信息
- 不同的动作 token 可以关注观测的不同部分

这就像：AdaLN 是老板对所有员工下同样的指令；cross-attention 是每个员工根据自己需要去查阅资料。

---

## 第七章：验证假说——在 UWM 内部替换 AdaLN 为 Cross-Attention

### 7.1 Phase A：Cross-Attention 替代实验

**这是整个项目最关键的实验。** 在 UWM 框架内，保留所有其他组件（VAE latent、DualTimestepEncoder、MultiViewVideoPatchifier、联合 loss），**仅将 AdaLN 条件化替换为 cross-attention**。

具体实现：
- 观测编码器输出多个 token（18 个图像 token + 2 个 agent_pos token = 20 个 memory token）
- 每个 AdaLNHybridAttentionBlock 中：self-attention（含 AdaLN 自调制）+ cross-attention（动作/视频 token 查询观测 memory）+ MLP

训练 5 个变体，统一 20k 步，确定性评估 50 个种子：

| 模型 | 条件化 | 视频头 | 分数 |
|------|------|:---:|:---:|
| AdaLN joint C | AdaLN | 有 | 0.434 |
| AdaLN DP-only C | AdaLN | 无 | 0.391 |
| Cross-Attn joint C (标准采样) | Cross-Attn | 有 | 0.354 |
| Cross-Attn joint C (联合去噪采样) | Cross-Attn | 有 | 0.383 |
| **Cross-Attn DP-only C** | **Cross-Attn** | **无** | **0.491** |

### 7.2 Phase A 的关键结论

1. **Cross-attention 机制本身有效**：CA DP-only（0.491）超过了所有 AdaLN 变体。从 0.391 到 0.491，+0.100，验证了条件化机制是瓶颈。

2. **视频分支在 cross-attention 下反而有害**：将视频 token 加入 CA 联合训练后，分数从 0.491 暴跌到 0.354（Δ=-0.136，**p=0.011**）。这是整个实验中最强的信号。

3. **推理时的修复不够**：使用联合去噪（同时去噪动作和视频 token）只恢复了 +0.029（p=0.53），说明问题主要在训练阶段——视频 token 和视频 loss 在训练时干扰了动作学习。

---

## 第八章：寻找平衡——小视频 loss 的正则化效应

### 8.1 梯度冲突诊断

Phase A 告诉我们两件事：
1. AdaLN 不是好条件化，cross-attention 更好
2. 但 cross-attention 下视频分支又严重拖累动作

为什么视频 loss 会有害？运行了梯度诊断：

**在 Cross-Attn joint C checkpoint 上，同时计算 action loss 和 dynamics loss 对模型参数的梯度**：

| 指标 | 数值 |
|------|:---:|
| action loss 均值 | 0.026 |
| dynamics loss 均值 | 0.035 |
| 两个梯度模长的比值（中位数） | ~1.06 |
| **梯度夹角余弦值（均值）** | **0.016** |
| 余弦值为负的比例 | 41.2% |
| 余弦值<0.3 的比例 | 97.8% |

**这意味着两个 loss 的梯度方向几乎正交（cos=0.016 接近 0 = 完全不相关），有 41% 的情况下梯度方向甚至是相反的（cos<0）。**

这不是梯度大小不匹配的问题，而是**方向性冲突**——优化动作预测和优化视频预测指向了几乎不相关的参数更新方向。两个目标同时优化时，互相拉扯。

### 8.2 思路转变：小视频 loss 作为正则化器

如果大的 video loss（λ=1.0）会破坏动作学习，那么一个很小的 video loss 会怎样？

**假说**：一个很小的 video/dynamics loss 可能起到正则化作用——它不驱动真正的视频预测学习，但它的微小梯度可以防止模型在纯动作 loss 上过拟合。

### 8.3 微调实验

从 R1 checkpoint（cross-attention joint 训练，但 video loss 权重 λ=0，即只训练动作，分数 0.534）出发，分别用不同的 λ 继续训练 5k 步：

| 实验 | λ | 分数 | 变化 |
|------|:---:|:---:|:---:|
| R1 @20k（基点） | 0.00 | 0.534 | — |
| cont-0.00（继续纯动作训练） | 0.00 | 0.450 | **下降 -0.084** |
| ft-0.01（加微小 video loss） | 0.01 | ~0.31 | 死区 |
| ft-0.03 | 0.03 | 0.504 | 接近保持 |
| **ft-0.05** | **0.05** | **0.537** | **保持且略优** |

**关键发现**：
- 继续纯动作训练（λ=0）反而导致退化（0.534 → 0.450），说明模型在过拟合
- λ=0.05 可以**阻止退化**，分数保持在 0.537

### 8.4 从零训练：λ=0.05 成为新记录

既然 λ=0.05 能在微调中防止过拟合，那从零开始就用 λ=0.05 会怎样？

| 实验 | λ 调度 | 分数 |
|------|------|:---:|
| joint λ=1.0（参照） | 全程 1.0 | 0.354 |
| R1 λ=0.0（参照） | 全程 0.0 | 0.534 |
| ft-0.05（微调） | 前 20k λ=0，+5k λ=0.05 | 0.537 |
| Warmup 0→0.05 | 前 10k λ=0，后 10k λ=0.05 | 0.241（失败） |
| **Scratch λ=0.05** | **全程 0.05** | **0.614** |

**0.614 — UWM 家族新最高分。**

配对统计：
- vs joint-C（λ=1.0）：+0.259，**p<0.001**（显著）
- vs R1（λ=0.0）：+0.080，p=0.263（趋势）
- vs ft-0.05：+0.076，p=0.199（趋势）

### 8.5 为什么 λ=0.05 有效？

关键在于梯度冲突的结构：

- λ=1.0 时，两个 loss 的梯度大小相当但方向几乎正交，训练过程中互相撕扯，谁都不好
- λ=0.0 时，只有动作 loss，模型可以通过记忆训练轨迹来降低 loss，导致闭环稳定性退化
- λ=0.05 时，video loss 梯度被缩小 20 倍，变得很小——它不足以驱动真正的视频预测，但**足以提供一个微弱的正则化信号**，防止模型在动作 loss 上过度拟合

**本质上是"让 video loss 退化为一个辅助正则化器，而非平等的多任务目标。"**

而 warmup（中途从 λ=0 切换到 λ=0.05）失败了，因为突然引入的梯度冲突打乱了已经收敛的策略。

---

## 第九章：实验全景——所有模型的最终排名

| 模型 | 架构 | 参数量 | 观测 | 条件化 | 分数 |
|------|------|:---:|------|------|:---:|
| E4 KP-TfD | TransformerDecoder 8L | 5M | 20D 关键点 | cross-attention | **0.998** |
| DP 官方 Image | UNet 1D + FiLM | ~287M | 图像 | global cond | **0.949** |
| B3 local DiT | TransformerEncoder 6L | 5M | 20D 关键点 | obs-as-token | **0.722** |
| **Scratch λ=0.05** | **Cross-Attn AdaLN-DiT 12L** | **247M** | **图像** | **cross-attention** | **0.614** |
| R1 λ=0.0 | Cross-Attn AdaLN-DiT 12L | 247M | 图像 | cross-attention | 0.534 |
| CA DP-only C | Cross-Attn AdaLN-DiT 12L | 247M | 图像 | cross-attention | 0.491 |
| UWM joint C | AdaLN-DiT 12L | 150M | 图像 | AdaLN | 0.434 |
| DP-only C | AdaLN-DiT 12L | 150M | 图像 | AdaLN | 0.391 |

**分级解读**：
- **第一梯队（>0.9）**：DP 官方 UNet（0.949，图像输入）和 E4 小型 Transformer（0.998，20D 关键点输入），分别代表了"正确架构 + 大参数 + 图像端到端"和"正确架构 + 小参数 + 特权状态输入"两条路径。**注意**：E4 和 B3 使用 20D 关键点输入（T 块的空间坐标已显式给出），本质上是 privileged state/keypoint 条件，而非从图像像素中端到端识别。因此 E4/B3 的主要作用是验证数据、评估协议和 Transformer 条件化路线的正确性，不能作为与图像策略完全公平的同模态比较。
- **第二梯队（0.6-0.8）**：B3（0.722，关键点输入）和 Scratch λ=0.05（0.614，图像输入），代表了不同路线下的最佳努力——但距离第一梯队仍有明显差距
- **第三梯队（0.39-0.54）**：所有 AdaLN 变体和早期的 cross-attention 变体（均图像输入），受困于架构瓶颈或 gradient conflict

---

## 第十章：破局思路总结——诊断方法论

### 10.1 从"猜测"到"排除"

整个项目最核心的方法论是：**不猜测，做受控实验，逐一排除。**

最初的怀疑清单有 7 项。通过 15 个模型的系统对比，一项一项排除了：
- ✅ 工程 bug → 修复后仍然差距大
- ✅ 视觉编码器 → 换了 keypoint 还是卡 0.44
- ✅ 模型规模 → 小模型反而更差
- ✅ 视频 head → 去掉了没显著变化
- ✅ EMA/LR 食谱 → 加了几乎没帮助
- ✅ 数据/eval → 外部正对照拿到 0.998

在本项目的 PushT 小数据、从随机初始化训练的设置下，证据强烈指向 AdaLN 全局条件化是主要瓶颈之一；更准确地说，是**"全局观测压缩 + token 共享调制"的条件注入方式**不适合 PushT 这类强空间几何任务。这一结论不应直接外推到 UWM 原论文的大规模预训练、多任务场景——UWM 论文自身的消融实验反而显示 cross-attention 在 LIBERO 设置中弱于 AdaLN。

### 10.2 "数据少，用小模型"的直觉

B3 和 E4 的成功说明了一个重要直觉：

PushT 的训练集只有 90 条轨迹（完整数据集 206 条），约 11,250 个 (观测, 动作) 对。对于深度学习来说，这是极小的训练集。此时：
- 305M 参数的大模型（UWM AdaLN-DiT）严重过参数化，但又因为条件化机制的问题无法有效利用这些参数
- 5M 参数的小模型（E4）配合正确的交叉注意力机制，参数效率极高，每条轨迹能支撑约 55k 参数的有效学习

**在数据有限时，架构的归纳偏置远比参数数量重要。** 正确的归纳偏置（因果自注意力 + cross-attention 条件化）可以在 5M 参数上达到接近满分；错误的归纳偏置（AdaLN 全局调制）即使给 150M 参数也只能到 0.44。

### 10.3 "外部正对照"的价值

B3 和 E4 不修改 UWM 的任何代码，只验证"数据和评估没问题"。它们在整个诊断中的价值不可替代：

- 如果没有 B3 拿到 0.722，我们可能还在怀疑"是不是 PushT 数据本身不适合基于 Transformer 的策略"
- 如果没有 E4 拿到 0.998，我们可能还在纠结"是不是 evaluation 脚本有隐藏 bug"

**用一个干净的外部参照来确认系统边界的正确性，是复杂系统调试的核心技巧。**

### 10.4 "梯度诊断"的洞察

梯度冲突诊断（cos(grad_act, grad_dyn) ≈ 0.016）提供了一个从"黑盒分数对比"到"白盒梯度分析"的跨越：

- 不仅能告诉你"joint training 不好"（Phase A 已经说了分数低）
- 还能告诉你**为什么**不好——两个目标在梯度空间几乎正交

这直接催生了 λ=0.05 的策略——不是盲目调参，而是理解了冲突结构后做出的精确响应。

---

## 第十一章：遗留问题与下一步

### 11.1 Cross-Attention 下视频分支的问题未解决

Phase A 发现 cross-attention + joint training（λ=1.0）只有 0.354，远低于 CA DP-only 的 0.491。梯度冲突解释了原因，但 λ=0.05 的 Scratch 训练（0.614）本质上是用 video loss 当正则化器，而不是真正让它学习视频预测。

**真正的 joint training（同时学到好的动作和好的视频预测）是否可能？** 这取决于能否解决梯度冲突问题。可能的方案包括：
- 梯度投影（将 video loss 梯度投影到 action loss 梯度的正交补空间）
- 交替训练（固定一个，训练另一个）
- 不同学习率或不同优化器

### 11.2 缩小与第一梯队的剩余差距

Scratch λ=0.05（0.614）距离 B3（0.722）还差 0.108，距离 DP 官方（0.949）差 0.335。差距可能来自：
- **推理步数**：当前 10 步 DDIM，E4 用 100 步 DDPM。100 步推理在 cross-attention UWM 上未完整测试
- **EMA**：在 cross-attention 条件下 EMA 是否更有效？
- **LR 调度**：cosine + warmup 在 cross-attention 下是否比常数 LR 更好？
- **注意力 mask**：UWM 当前使用双向自注意力，E4 使用因果 mask。因果 mask 提供了更好的物理归纳偏置

### 11.3 是否值得继续优化 UWM？

这是一个需要判断的问题。当前 UWM 的最优方案（CA + λ=0.05，0.614）是 AdaLN baseline（0.434）的 1.4 倍，但仍然远低于：
- 同架构的小型 cross-attention Transformer（0.722-0.998）
- 不同架构的 DP 官方 UNet（0.949）

**核心权衡**：UWM 的价值主张是"联合视频/动作预测 + 跨任务泛化"。但如果连单任务 PushT 上都达不到竞争分数，多任务泛化的价值就无法体现。

可能的两个方向：
1. **继续修 UWM**：完成 recipe 对齐（100 步推理、EMA、cosine LR、因果 mask），看能否追到 0.8+
2. **放弃 UWM 的 AdaLN 遗产**：直接用 DP 官方 TransformerForDiffusion 替代 UWM 的动作骨干，在正确的架构基础上重新引入视频预测

---

## 附录：关键文件索引

### 训练脚本
- `unified-world-model-main/experiments/uwm/train_pusht.py` — UWM PushT 训练主脚本（支持 --conditioning-type, --dp-only, --dynamics-loss-weight）
- `scripts/kp_transformer_for_diffusion.py` — E3/E4 外部正对照训练脚本
- `scripts/b3_train_20d_dit.py` — B3 外部正对照训练脚本

### 评估脚本
- `unified-world-model-main/experiments/uwm/eval_pusht.py` — UWM PushT 评估
- `scripts/deterministic_paired_eval.py` — 确定性成对评估（50 个相同种子，严格可对比）

### 诊断脚本
- `scripts/diag_loss_grad_scale.py` — 梯度冲突诊断
- `scripts/diag_rollout_trajectory.py` — 单回合详细状态追踪
- `scripts/analyze_paired_eval.py` — 统计分析（t 检验、Wilcoxon、bootstrap）

### 核心模型代码
- `unified-world-model-main/models/uwm/uwm.py` — UnifiedWorldModel + DualNoisePredictionNet
- `unified-world-model-main/models/uwm/obs_encoder.py` — UWMObservationEncoder
- `unified-world-model-main/models/common/adaln_attention.py` — AdaLNAttentionBlock / AdaLNHybridAttentionBlock

### 数据集
- `unified-world-model-main/datasets/pusht/dataset.py` — PushTDataset
- 数据文件：`pusht_cchi_v7_replay.zarr`（完整 206 条轨迹，DP 官方训练集取前 90 条）

### 实验产出
- `outputs/` — 所有训练、评估、诊断输出（46 个子目录）
- `artifacts_keep/` — 关键 checkpoint 和结果备份
- `final_visual_summary_verified/` — 最终图表和视频
